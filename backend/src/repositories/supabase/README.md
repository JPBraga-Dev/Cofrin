# Adaptador Supabase

Implemente aqui os repositórios que atendem às interfaces em `../interfaces.ts`. Os services não devem importar o cliente Supabase; a troca é feita pela composição em `config/dataSource.ts` quando `DATA_SOURCE=supabase`.
