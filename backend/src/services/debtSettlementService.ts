import type { Settlement } from '../domain/types.js';
const cents=(value:number)=>Math.round(value*100+(value>=0?1e-8:-1e-8));
export function calculateSettlements(balances:{userId:string;balance:number}[]):Settlement[]{const debtors=balances.filter(x=>cents(x.balance)<0).map(x=>({userId:x.userId,amount:-cents(x.balance)})).sort((a,b)=>b.amount-a.amount);const creditors=balances.filter(x=>cents(x.balance)>0).map(x=>({userId:x.userId,amount:cents(x.balance)})).sort((a,b)=>b.amount-a.amount);const result:Settlement[]=[];let debtor=0,creditor=0;while(debtor<debtors.length&&creditor<creditors.length){const amount=Math.min(debtors[debtor].amount,creditors[creditor].amount);result.push({fromUserId:debtors[debtor].userId,toUserId:creditors[creditor].userId,amount:amount/100,status:'SUGGESTED'});debtors[debtor].amount-=amount;creditors[creditor].amount-=amount;if(debtors[debtor].amount===0)debtor++;if(creditors[creditor].amount===0)creditor++;}return result}
